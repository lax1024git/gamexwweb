import { defineConfig } from "unocss";

export default defineConfig({
  // ...UnoCSS options
  
  shortcuts: [
    {
      "border-base": "border border-solid border-gray-500:10",
      "center": "flex justify-center items-center",
    },
  ],
});